{
  "name": "@media-bot/upload",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Upload layer - MinIO (S3), Google Drive via rclone",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@media-bot/core": "workspace:*",
    "@media-bot/utils": "workspace:*",
    "minio": "^7.1.3",
    "pino": "^8.17.0"
  },
  "devDependencies": {
    "@types/minio": "^7.1.1",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  }
}
