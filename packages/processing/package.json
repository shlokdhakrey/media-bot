{
  "name": "@media-bot/processing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Media processing and muxing - FFmpeg command execution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@media-bot/core": "workspace:*",
    "@media-bot/media": "workspace:*",
    "@media-bot/sync": "workspace:*",
    "@media-bot/utils": "workspace:*",
    "pino": "^8.17.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  }
}
