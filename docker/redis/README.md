# Redis Docker Configuration

Custom Redis configuration for the media bot.

## Files

- `redis.conf` - Redis configuration

## Features

- Persistence enabled (AOF + RDB)
- Max memory configured
- Optimized for BullMQ queues
